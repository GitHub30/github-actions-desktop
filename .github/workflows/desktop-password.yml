name: Desktop Password

on:
  workflow_dispatch:
    inputs:
      password:
        description: 'Password ( username: abc )'
        required: true
        default: password

jobs:
  desktop:
    runs-on: ubuntu-latest
    steps:
      - name: 'Start Docker ( username: abc )'
        background: true
        run: |
          docker run -d \
            --network host \
            --security-opt seccomp=unconfined \
            --shm-size=1gb \
            --restart unless-stopped \
            -e PASSWORD=${{ inputs.password }} \
            ghcr.io/linuxserver/webtop

      - name: Desktop URL https://<random-subdomain>.trycloudflare.com
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb >/dev/null 2>&1
          cloudflared tunnel --url http://localhost:3000
